import 'package:flutter/material.dart';

class UserChangePoints extends StatelessWidget {
  const UserChangePoints({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('change points');
  }
}
